package Project1;

public enum Qualification {/**
	 * 
	 *
	 * This class defines some (public) fixed objects that represent a qualification.
	 * 
	 * DO NOT ALTER THIS MODULE
	 * 
	 * You can write expressions like:
	 *   if (student.getQualification() == Qualification.FAIL) ...
	 *   
	 *   The above expression is comparing the object returned by a getQualification() method call
	 *   with the fixed object that represents a qualification, and if they are the same object ...
	 *   
	 */
	
	    MSC_WITH_DISTINCTION, MSC_WITH_MERIT, MSC, FAIL
	


}
